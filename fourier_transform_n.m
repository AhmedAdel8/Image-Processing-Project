function [fi] = fourier_transform_n(image)
fi   = fft2(image) ;
fshift = fftshift(fi);
fabs =abs(fshift) ; % ????? ?? ??? ??imagin%
flog = log(fabs+1) ;%????? ???? ?????? ??????
fm = mat2gray(flog) ;%normalization%
fi=fm;
end

